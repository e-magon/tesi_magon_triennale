regex_list = [
    # Elenco di regex da applicare manualmente ai messaggi
    # di log per rilevare dati sensibili.

    {
        "name": "email_regex",
        "regex": r"[^\s@]+@[^\s@]+\.[^\s@]+",
    },
]
